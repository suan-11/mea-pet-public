
# 虚拟环境
.venv/
venv/
env/
_python/

# Python 缓存
__pycache__/
*.py[cod]
*.pyo
*.py.back
*.back

# 配置文件（可能含密钥）
config.json
config_settings.json
config_settings.json.bak
config*.bak*
openclaw_device_identity.json
pet_window_state.json
wizard_window_state.json

# 日志与本地运行数据
*.log
*.log.*
logs/
*.download
*.tmp
*.db
*.db-journal
*.db-wal
*.db-shm
mea_memory.db
tts_debug.log
chat_errors.log

# 运行时缓存 / 下载物
audio_cache/
voice_cache/
voice_asr/
models/voice_asr/
screenshots/
wheels/
OllamaSetup.exe
_python.zip

# 测试 / 静态检查缓存
.coverage*
.pytest_cache/
.mypy_cache/
.ruff_cache/

# 操作系统文件
.DS_Store
Thumbs.db

# IDE 配置
.vscode/
.idea/

# Local assistant/developer instructions are not project artifacts.
/AGENTS.md
/CLAUDE.md

# 构建产物
mea_pet.egg-info/
dist/
build/
debug_grab_frame.png

# 误提交的运行时/调试残留文件
/_standby=True
/stop

# ===== C / Wayland 原生层编译产物 =====
# 目标文件
*.o
*.obj

# 共享库 / 静态库（由 build_layer_shell.sh 生成）
*.so
*.so.*
*.a
*.dylib

# 验证程序可执行文件（无扩展名，单独点名）
/minimal_layer_shell

# ===== Rust 原生层（native/layer_shell，spec §5.1）=====
# 构建目录不入库；Cargo.lock 必须入库（复现）
native/layer_shell/target/
scripts/rust_layer_probe/target/

# AI/编辑器本地会话与偏好（含私有 sessions/，不入库）
.qoder/

# 调试 / 验证脚本（可选保留，视团队习惯）
/test_layer_pixels.py

# ===== 以下内容必须提交，不能忽略 =====
# layer_shell_c.c / layer_shell_c.h / layer_shell_shim.cpp   —— 手写源码
# wlr-layer-shell-client-protocol.c/.h                       —— 协议生成代码
# xdg-shell-client-protocol.c/.h                             —— 协议生成代码
# build_layer_shell.sh                                       —— 构建脚本
# minimal_layer_shell.c                                      —— 最小验证程序源码（文档引用）

